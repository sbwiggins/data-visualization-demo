import { RelatedArchitecture } from './related-architecture';

describe('RelatedArchitecture', () => {
  it('should create an instance', () => {
    expect(new RelatedArchitecture()).toBeTruthy();
  });
});
